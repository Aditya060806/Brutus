node_modules
dist
out
.vite/

.DS_Store
.eslintcache
*.log*

.env
.env.*
!.env.example


# Editor, IDE, and agent config — local to each machine, not shared.
.idea/
.vscode/
.claude/

# GitHub repo metadata (issue/PR templates, Dependabot, funding).
# NOTE: ignoring this disables those GitHub features for the repo.
.github/


credentials.json
gmail_token.json

eng.traineddata

versions/
versions.json
**/versions.json

# Dev artifacts
npm-errors.txt
electron.vite.config.*.mjs
BRUTUS_CODEBASE_SUMMARY.md
*.traineddata
# Test bundles (regenerate with: node tests/build.mjs)
tests/**/*.test.cjs
HANDOFF.md

# Bundled ML models — build output, fetched by `npm run fetch:models`.
# Large binaries that must never enter git; electron-builder copies them into
# the installer via `extraResources`.
resources/models/*
!resources/models/README.md
